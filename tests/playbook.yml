---
- hosts: apache-xenial
  gather_facts: false
  roles: [debian-bootstrap]

- hosts: all
  strategy: free
  roles:
    - role: adarnimrod.ca-store
    - role: apache
  post_tasks:
      - name: Install cURL
        package:
            name: curl
            state: present
